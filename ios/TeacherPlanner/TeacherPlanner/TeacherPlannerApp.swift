// =============================================================================
// TeacherPlannerApp.swift - App Entry Point
// =============================================================================
// The main entry point for the Teacher Planner iOS app.
//
// @main marks this as the app's entry point.
// The App protocol requires a body property that returns a Scene.
// WindowGroup creates a window containing our ContentView.
// =============================================================================

import SwiftUI

@main
struct TeacherPlannerApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}
